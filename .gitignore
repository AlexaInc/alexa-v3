# Ignore node modules
node_modules/

auth5a/
# Ignore environment variables
.env

# (Optional) Ignore logs, system files, etc.
*.log
.DS_Store

# --- Runtime state / caches (recreated on start) ---

# Baileys session files (NEVER COMMIT THIS)
baileys_auth_info/

# Bot data stores and cache
store/
filters/
store_ev/
data/
msgstore.json

# Specific temporary files to ignore
111.webp
1234.webp
pp.jpg
sticker.png
assets/img/alexaold\ .jpeg

# Local scratch dir (cookies, etc. — never commit credentials)
.local/

# Cookies / credential files (leaked YouTube cookies were committed before!)
**/cookies.txt

# Generated proxy config (contains proxy credentials)
xray_config.json

# Bot log files
wa-logs.txt
test_out.txt

botstatus.json
downloads/
quizzes/
temp/
ex.js
shipping/
database/
authtest/
